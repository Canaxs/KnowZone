server:
  servlet:
    context-path: "/"

spring:
  profiles:
    active: ${PROFILE_ACTIVE}
  jpa:
    show-sql: false


openai:
  url: "https://api.openai.com/v1/chat/completions"
  key: ${OPENAI_KEY}